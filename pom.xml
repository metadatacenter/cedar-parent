<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.metadatacenter</groupId>
  <artifactId>cedar-parent</artifactId>
  <version>2.9.8-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>CEDAR Parent</name>
  <description>Parent project for all Java-based CEDAR components</description>
  <url>https://github.com/metadatacenter</url>
  <properties>
    <cedar.version>2.9.8-SNAPSHOT</cedar.version>
    <dropwizard.version>5.0.2</dropwizard.version>
    <dropwizard.metrics.version>4.2.40</dropwizard.metrics.version>
    <mongodb.version>5.1.2</mongodb.version>
    <keycloak.version>22.0.4</keycloak.version>
    <keycloak-admin-client-jakarta.version>21.1.2</keycloak-admin-client-jakarta.version>
    <opensearch.version>2.19.2</opensearch.version>
    <org.neo4j.driver.version>5.28.14</org.neo4j.driver.version>
    <mysql.connector.j.version>8.4.0</mysql.connector.j.version>
    <org.xerial.sqlite-jdbc.version>3.53.4.0</org.xerial.sqlite-jdbc.version>
    <redis.clients.jedis.version>5.2.0</redis.clients.jedis.version>
    <jsonld.version>0.13.6</jsonld.version>
    <swagger.version>2.2.55</swagger.version>
    <org.eclipse.jetty.version>12.1.12</org.eclipse.jetty.version>
    <!-- Keep every Jackson artifact on the version in the Dropwizard 5.0.2 dependency bundle. -->
    <jackson.core.version>2.22.2</jackson.core.version>
    <networknt.json.schema.validator.version>1.5.9</networknt.json.schema.validator.version>
    <resteasy.version>6.2.4.Final</resteasy.version>
    <google-guava.version>33.7.1-jre</google-guava.version>
    <weka.version>3.8.7</weka.version>
    <org.glassfish.jaxb.jaxb-runtime.version>4.0.9</org.glassfish.jaxb.jaxb-runtime.version>
    <commons.io.version>2.22.0</commons.io.version>
    <commons.lang3.version>3.20.0</commons.lang3.version>
    <commons.codec.version>1.22.1</commons.codec.version>
    <commons.text.version>1.15.0</commons.text.version>
    <!-- Aligned with the Dropwizard 5.0.2 dependency bundle. -->
    <apache.httpcomponents.core.version>5.4.2</apache.httpcomponents.core.version>
    <apache.httpcomponents.httpclient.version>5.6.1</apache.httpcomponents.httpclient.version>
    <apache.httpcomponents.httpclient-fluent.version>5.6.1</apache.httpcomponents.httpclient-fluent.version>
    <!-- HttpClient 4, kept only for the OpenSearch REST client and jsonld-java. These match the
         versions the OpenSearch client resolves, so pinning them changes nothing that was already
         winning on the classpath; it removes the older duplicate. -->
    <apache.httpcomponents.legacy.core.version>4.4.16</apache.httpcomponents.legacy.core.version>
    <apache.httpcomponents.legacy.httpclient.version>4.5.14</apache.httpcomponents.legacy.httpclient.version>
    <apache.commons.fileupload2.version>2.0.0-M5</apache.commons.fileupload2.version>
    <apache.commons.net.version>3.13.0</apache.commons.net.version>
    <apache.commons.cli.version>1.11.0</apache.commons.cli.version>
    <apache.commons.validator.version>1.11.0</apache.commons.validator.version>
    <apache.poi.version>5.5.1</apache.poi.version>
    <java.dev.jna.version>5.19.1</java.dev.jna.version>
    <javax.ws-api.version>2.3.1</javax.ws-api.version>
    <com.sun.activation.version>2.0.1</com.sun.activation.version>
    <hibernate-core.version>6.6.52.Final</hibernate-core.version>
    <jakarta.persistence-api.version>3.1.0</jakarta.persistence-api.version>
    <jakarta.servlet-api.version>6.0.0</jakarta.servlet-api.version>
    <jakarta.validation-api.version>3.0.2</jakarta.validation-api.version>
    <jakarta.xml.bind-api.version>4.0.5</jakarta.xml.bind-api.version>
    <com.jayway.jsonpath.version>2.9.0</com.jayway.jsonpath.version>
    <org.javassist.version>3.33.0-GA</org.javassist.version>
    <net.sourceforge.owlapi.version>4.5.9</net.sourceforge.owlapi.version>
    <me.xdrop.fuzzywuzzy.version>1.4.0</me.xdrop.fuzzywuzzy.version>
    <junit.jupiter.version>5.11.3</junit.jupiter.version>
    <org.hamcrest.core.version>2.2</org.hamcrest.core.version>
    <org.mockito.version>5.23.0</org.mockito.version>
    <!-- The embedded backends the backend-free suites run against. neo4j-harness remains at 5.3.0:
         5.26.0 brings Lucene 9.11.1, whose Lucene99Codec layout is incompatible with OpenSearch's
         parent-managed Lucene 9.12.1 on the shared server test classpath; the harness fails before
         the database starts. The independently versioned driver stays on its compatible 5.28
         maintenance line. embedded-redis 1.4.3 bundles Redis 6.2.x and has no Redis 7.2 binary.
         These suites therefore provide backend-free graph, command, and queue coverage, not deployed
         Neo4j 5.26.0 / Redis 7.2.7 parity; those semantics require the real-stack smoke until the
         helpers are isolated or replaced. Flapdoodle and MariaDB4j provide the Mongo and MariaDB
         test engines. -->
    <com.github.codemonstur.embedded-redis.version>1.4.4</com.github.codemonstur.embedded-redis.version>
    <ch.vorburger.mariadb4j.version>3.3.1</ch.vorburger.mariadb4j.version>
    <ch.vorburger.mariadb4j.db.version>11.4.5</ch.vorburger.mariadb4j.db.version>
    <de.flapdoodle.embed.mongo.version>4.20.0</de.flapdoodle.embed.mongo.version>
    <org.neo4j.test.neo4j-harness.version>5.3.0</org.neo4j.test.neo4j-harness.version>
    <org.jspecify.version>1.0.1</org.jspecify.version>
    <!-- The javax-era XML binding stack, still on the classpath of the cadsr and impex tooling and
         the terminology ingest. Kept apart from the jakarta artifacts above, which carry different
         coordinates and so can be managed independently. -->
    <com.sun.xml.bind.version>3.0.0</com.sun.xml.bind.version>
    <javax.xml.bind.jaxb-api.version>2.3.1</javax.xml.bind.jaxb-api.version>
    <javax.activation-api.version>1.2.0</javax.activation-api.version>
    <!-- Versions match the Dropwizard 5.0.2 dependency bundle. -->
    <slf4j.version>2.0.18</slf4j.version>
    <logback.version>1.5.33</logback.version>
    <jboss.logging>3.6.3.Final</jboss.logging>
    <log4j.version>2.24.3</log4j.version>
    <org.yaml.snakeyaml.version>2.7</org.yaml.snakeyaml.version>
    <google.code.findbugs.version>3.0.2</google.code.findbugs.version>
    <maven.version>3.0.5</maven.version>
    <maven.enforcer.plugin.version>3.6.3</maven.enforcer.plugin.version>
    <maven.surefire.plugin.version>3.6.0</maven.surefire.plugin.version>
    <maven.compiler.plugin.version>3.8.0</maven.compiler.plugin.version>
    <maven.jsonschema2pojo.plugin.version>1.0.0</maven.jsonschema2pojo.plugin.version>
    <maven.jax.plugin.version>0.1.5</maven.jax.plugin.version>
    <maven.build-helper.plugin.version>3.6.1</maven.build-helper.plugin.version>
    <maven.dependency.plugin.version>3.11.0</maven.dependency.plugin.version>
    <maven.exec.plugin.version>3.6.3</maven.exec.plugin.version>
    <maven.assembly.plugin.version>3.8.0</maven.assembly.plugin.version>
    <maven.resources.plugin.version>3.3.1</maven.resources.plugin.version>
    <maven.install.plugin.version>3.1.1</maven.install.plugin.version>
    <maven.javadoc.plugin.version>3.12.0</maven.javadoc.plugin.version>
    <maven.scm.gitexe.version>1.11.1</maven.scm.gitexe.version>
    <maven.failsafe.plugin.version>3.6.0</maven.failsafe.plugin.version>
    <maven.shade.plugin.version>3.6.2</maven.shade.plugin.version>
    <log4j.transform.shade.extensions.version>0.2.0</log4j.transform.shade.extensions.version>
    <!-- Guard for the managed cedar-shade execution below. Maven merges pluginManagement
         executions into every pom that declares the plugin, so the execution defaults to the
         inert phase "none". A pom opts in by setting cedar.shade.phase to "package" and
         cedar.shade.main.class to its application class. -->
    <cedar.shade.phase>none</cedar.shade.phase>
    <maven.deploy.plugin.version>3.1.1</maven.deploy.plugin.version>
    <maven.versions.plugin.version>2.21.0</maven.versions.plugin.version>
    <maven.clean.plugin.version>3.3.1</maven.clean.plugin.version>
    <maven.site.plugin.version>3.12.1</maven.site.plugin.version>
    <jaxb2.maven.plugin.version>3.1.0</jaxb2.maven.plugin.version>
    <jvnet.jaxb.maven.plugin.version>4.0.16</jvnet.jaxb.maven.plugin.version>
    <spotless.maven.plugin.version>2.43.0</spotless.maven.plugin.version>
    <maven.jar.plugin.version>3.5.0</maven.jar.plugin.version>
    <maven.source.plugin.version>3.3.0</maven.source.plugin.version>
    <maven.central-publishing.plugin.version>0.11.0</maven.central-publishing.plugin.version>
    <maven.release.plugin.version>2.5.3</maven.release.plugin.version>
    <maven.gpg.plugin.version>3.0.1</maven.gpg.plugin.version>
    <java.version>17</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <!-- Versions of artifacts that reach this estate only transitively. Each one resolved to more
         than one version across the build before it was managed here, meaning the choice fell to
         whichever upstream pom happened to sit nearest. Managed so that an upstream release cannot
         move a shipped artifact on its own. -->
    <transitive.netty.version>4.1.115.Final</transitive.netty.version>
    <transitive.lucene.version>9.12.1</transitive.lucene.version>
    <transitive.jersey.version>3.1.11</transitive.jersey.version>
    <transitive.hk2-locator.version>3.0.6</transitive.hk2-locator.version>
    <transitive.caffeine.version>3.2.4</transitive.caffeine.version>
    <transitive.classmate.version>1.7.3</transitive.classmate.version>
    <transitive.zstd-jni.version>1.5.7-16</transitive.zstd-jni.version>
    <transitive.error-prone-annotations.version>2.50.0</transitive.error-prone-annotations.version>
    <transitive.protobuf-java.version>3.25.5</transitive.protobuf-java.version>
    <transitive.istack-commons-runtime.version>4.2.0</transitive.istack-commons-runtime.version>
    <transitive.commons-logging.version>1.4.0</transitive.commons-logging.version>
    <transitive.reactor-core.version>3.5.20</transitive.reactor-core.version>
    <transitive.jakarta-activation-api.version>2.1.4</transitive.jakarta-activation-api.version>
    <transitive.jakarta-inject-api.version>2.0.1.MR</transitive.jakarta-inject-api.version>
    <!-- Mockito 5.23.0 declares Byte Buddy 1.17.7, close enough to the 1.18.8-jdk5 selected by
         dropwizard-hibernate to share one tested implementation. Keep the core and agent
         together: Mockito's inline mock maker uses both, and a mismatch can leave stubbing
         silently inert. -->
    <transitive.byte-buddy.version>1.18.13-jdk5</transitive.byte-buddy.version>
    <transitive.commons-compress.version>1.28.0</transitive.commons-compress.version>
    <transitive.angus-activation.version>2.0.3</transitive.angus-activation.version>
    <transitive.jandex.version>2.4.3.Final</transitive.jandex.version>
    <transitive.asm.version>9.10.1</transitive.asm.version>
  </properties>
  <distributionManagement>
    <snapshotRepository>
      <id>bmir-nexus-snapshots</id>
      <url>https://nexus.bmir.stanford.edu/repository/snapshots</url>
    </snapshotRepository>
    <repository>
      <id>bmir-nexus-releases</id>
      <url>https://nexus.bmir.stanford.edu/repository/releases</url>
    </repository>
  </distributionManagement>
  <scm>
    <connection>scm:git:https://github.com/metadatacenter/cedar-parent.git</connection>
    <developerConnection>scm:git:https://github.com/metadatacenter/cedar-parent.git</developerConnection>
    <url>https://github.com/metadatacenter/cedar-parent</url>
  </scm>
  <issueManagement>
    <system>GitHub</system>
    <url>https://waffle.io/metadatacenter/cedar-project</url>
  </issueManagement>
  <developers>
    <developer>
      <name>Attila Egyedi</name>
      <email>egyedia@stanford.edu</email>
      <organization>Stanford Center for Biomedical Informatics Research</organization>
      <organizationUrl>https://bmir.stanford.edu</organizationUrl>
    </developer>
    <developer>
      <name>Marcos Martinez-Romero</name>
      <email>marcosmr@stanford.edu</email>
      <organization>Stanford Center for Biomedical Informatics Research</organization>
      <organizationUrl>https://bmir.stanford.edu</organizationUrl>
    </developer>
    <developer>
      <name>Josef Hardi</name>
      <email>johardi@stanford.edu</email>
      <organization>Stanford Center for Biomedical Informatics Research</organization>
      <organizationUrl>https://bmir.stanford.edu</organizationUrl>
    </developer>
    <developer>
      <name>Martin O'Connor</name>
      <email>martin.oconnor@stanford.edu</email>
      <organization>Stanford Center for Biomedical Informatics Research</organization>
      <organizationUrl>https://bmir.stanford.edu</organizationUrl>
    </developer>
  </developers>
  <licenses>
    <license>
      <name>BSD 2-Clause License</name>
      <url>https://opensource.org/licenses/BSD-2-Clause</url>
    </license>
  </licenses>
  <dependencyManagement>
    <dependencies>
      <!--
        Aligns every org.junit.jupiter and org.junit.platform artifact on one JUnit 5 version,
        including the platform artifacts surefire resolves for its JUnit Platform provider.
      -->
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.jupiter.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.core.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-core</artifactId>
        <version>${logback.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-api</artifactId>
        <version>${log4j.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-core</artifactId>
        <version>${log4j.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.httpcomponents.core5</groupId>
        <artifactId>httpcore5</artifactId>
        <version>${apache.httpcomponents.core.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.httpcomponents.client5</groupId>
        <artifactId>httpclient5</artifactId>
        <version>${apache.httpcomponents.httpclient.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.httpcomponents.client5</groupId>
        <artifactId>httpclient5-fluent</artifactId>
        <version>${apache.httpcomponents.httpclient-fluent.version}</version>
      </dependency>
      <!-- HttpClient 4, which the stack no longer uses directly but cannot yet be rid of: the
           OpenSearch REST client's public API is built on it, so org.apache.http.HttpHost appears
           in our own signatures, and jsonld-java uses it to fetch remote JSON-LD contexts. Pinned
           here only to keep one copy of those classes on the classpath. See the jsonld-java entry
           below, which excludes the OSGi bundles that would otherwise supply a second, older copy.
           These pins go away when OpenSearch moves to HttpClient 5. -->
      <dependency>
        <groupId>org.apache.httpcomponents</groupId>
        <artifactId>httpcore</artifactId>
        <version>${apache.httpcomponents.legacy.core.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.httpcomponents</groupId>
        <artifactId>httpclient</artifactId>
        <version>${apache.httpcomponents.legacy.httpclient.version}</version>
      </dependency>
      <!-- jsonld-java's JsonUtils builds a CachingHttpClientBuilder, whose classes live here and
           not in httpclient. It reached them through httpclient-osgi; with that excluded it needs
           the plain artifact. -->
      <dependency>
        <groupId>org.apache.httpcomponents</groupId>
        <artifactId>httpclient-cache</artifactId>
        <version>${apache.httpcomponents.legacy.httpclient.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-net</groupId>
        <artifactId>commons-net</artifactId>
        <version>${apache.commons.net.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-cli</groupId>
        <artifactId>commons-cli</artifactId>
        <version>${apache.commons.cli.version}</version>
      </dependency>
      <!-- Jakarta-namespace successor to commons-fileupload for Jakarta Servlet 6. -->
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-fileupload2-jakarta-servlet6</artifactId>
        <version>${apache.commons.fileupload2.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-fileupload2-core</artifactId>
        <version>${apache.commons.fileupload2.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-validator</groupId>
        <artifactId>commons-validator</artifactId>
        <version>${apache.commons.validator.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-io</groupId>
        <artifactId>commons-io</artifactId>
        <version>${commons.io.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-lang3</artifactId>
        <version>${commons.lang3.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-codec</groupId>
        <artifactId>commons-codec</artifactId>
        <version>${commons.codec.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-text</artifactId>
        <version>${commons.text.version}</version>
      </dependency>
      <dependency>
        <groupId>com.networknt</groupId>
        <artifactId>json-schema-validator</artifactId>
        <version>${networknt.json.schema.validator.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>mongodb-driver-sync</artifactId>
        <version>${mongodb.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-common</artifactId>
        <version>${keycloak.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-core</artifactId>
        <version>${keycloak.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-adapter-core</artifactId>
        <version>${keycloak.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-admin-client-jakarta</artifactId>
        <version>${keycloak-admin-client-jakarta.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-server-spi</artifactId>
        <version>${keycloak.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-server-spi-private</artifactId>
        <version>${keycloak.version}</version>
      </dependency>
      <dependency>
        <groupId>org.keycloak</groupId>
        <artifactId>keycloak-services</artifactId>
        <version>${keycloak.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.code.findbugs</groupId>
        <artifactId>jsr305</artifactId>
        <version>${google.code.findbugs.version}</version>
      </dependency>
      <dependency>
        <groupId>me.xdrop</groupId>
        <artifactId>fuzzywuzzy</artifactId>
        <version>${me.xdrop.fuzzywuzzy.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.jupiter.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.hamcrest</groupId>
        <artifactId>hamcrest</artifactId>
        <version>${org.hamcrest.core.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${org.mockito.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>com.github.codemonstur</groupId>
        <artifactId>embedded-redis</artifactId>
        <version>${com.github.codemonstur.embedded-redis.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>ch.vorburger.mariaDB4j</groupId>
        <artifactId>mariaDB4j</artifactId>
        <version>${ch.vorburger.mariadb4j.version}</version>
      </dependency>
      <!--
        The platform database binaries, selected by an OS-activated profile in each consumer, since
        profiles do not travel with dependencies. Their version is the database, not the mariaDB4j
        wrapper, so it moves independently.
      -->
      <dependency>
        <groupId>ch.vorburger.mariaDB4j</groupId>
        <artifactId>mariaDB4j-db-macos-arm64</artifactId>
        <version>${ch.vorburger.mariadb4j.db.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.vorburger.mariaDB4j</groupId>
        <artifactId>mariaDB4j-db-linux64</artifactId>
        <version>${ch.vorburger.mariadb4j.db.version}</version>
      </dependency>
      <dependency>
        <groupId>ch.vorburger.mariaDB4j</groupId>
        <artifactId>mariaDB4j-db-winx64</artifactId>
        <version>${ch.vorburger.mariadb4j.db.version}</version>
      </dependency>
      <dependency>
        <groupId>de.flapdoodle.embed</groupId>
        <artifactId>de.flapdoodle.embed.mongo</artifactId>
        <version>${de.flapdoodle.embed.mongo.version}</version>
      </dependency>
      <dependency>
        <groupId>org.neo4j.test</groupId>
        <artifactId>neo4j-harness</artifactId>
        <version>${org.neo4j.test.neo4j-harness.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jspecify</groupId>
        <artifactId>jspecify</artifactId>
        <version>${org.jspecify.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.xml.bind</groupId>
        <artifactId>jaxb-core</artifactId>
        <version>${com.sun.xml.bind.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.xml.bind</groupId>
        <artifactId>jaxb-impl</artifactId>
        <version>${com.sun.xml.bind.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.xml.bind</groupId>
        <artifactId>jaxb-api</artifactId>
        <version>${javax.xml.bind.jaxb-api.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.activation</groupId>
        <artifactId>javax.activation-api</artifactId>
        <version>${javax.activation-api.version}</version>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-simple</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <!--
        The queue-service test fixture (EmbeddedRedis, QueueTestConfig), shared by every library and
        server that owns a queue service. Managed separately from the library's own jar because
        dependency management keys on the type as well as the coordinates, so the entry above for
        cedar-queue-operations-library does not cover its test-jar.
      -->
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-queue-operations-library</artifactId>
        <version>${cedar.version}</version>
        <type>test-jar</type>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.jboss.logging</groupId>
        <artifactId>jboss-logging</artifactId>
        <version>${jboss.logging}</version>
      </dependency>
      <dependency>
        <groupId>org.opensearch</groupId>
        <artifactId>opensearch</artifactId>
        <version>${opensearch.version}</version>
      </dependency>
      <dependency>
        <groupId>org.opensearch.client</groupId>
        <artifactId>opensearch-rest-high-level-client</artifactId>
        <version>${opensearch.version}</version>
      </dependency>
      <dependency>
        <groupId>net.java.dev.jna</groupId>
        <artifactId>jna</artifactId>
        <version>${java.dev.jna.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>${google-guava.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-server-utils-core-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-server-globals-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-server-utils-dropwizard-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-auth-operations-keycloak-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-auth-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-template-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-user-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-workspace-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-valuerecommender-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-schema-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-artifact-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-resource-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-group-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-repo-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-terminology-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-terminology-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-terminology-server-common</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-terminology-server-store</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-valuerecommender-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-valuerecommender-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-worker-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-worker-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-bridge-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-bridge-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-messaging-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-messaging-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-submission-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-submission-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-cadsr-tools</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-openview-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-openview-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-impex-server-core</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-impex-server-application</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-server-rest-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-test-support-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-search-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-inclusion-subgraph-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-logging-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-permissions-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-cache-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-queue-operations-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-core-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-model-validation-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-model-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-config-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.metadatacenter</groupId>
        <artifactId>cedar-artifact-library</artifactId>
        <version>${cedar.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jboss.resteasy</groupId>
        <artifactId>resteasy-client</artifactId>
        <version>${resteasy.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jboss.resteasy</groupId>
        <artifactId>resteasy-jackson2-provider</artifactId>
        <version>${resteasy.version}</version>
      </dependency>
      <dependency>
        <groupId>net.jmob</groupId>
        <artifactId>guice.conf</artifactId>
        <version>1.2.0</version>
      </dependency>
      <!-- The Jakarta Keycloak admin client needs the jakarta.ws.rs namespace, and Jersey drags in
           an older version of this artifact, so pin the 3.x line to keep the client's classes. -->
      <dependency>
        <groupId>jakarta.ws.rs</groupId>
        <artifactId>jakarta.ws.rs-api</artifactId>
        <version>3.1.0</version>
      </dependency>
      <!-- Jakarta EE 10 API artifacts aligned with Dropwizard 5. -->
      <dependency>
        <groupId>jakarta.servlet</groupId>
        <artifactId>jakarta.servlet-api</artifactId>
        <version>${jakarta.servlet-api.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.validation</groupId>
        <artifactId>jakarta.validation-api</artifactId>
        <version>${jakarta.validation-api.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.xml.bind</groupId>
        <artifactId>jakarta.xml.bind-api</artifactId>
        <version>${jakarta.xml.bind-api.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-core</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-jersey</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-jetty</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard.metrics</groupId>
        <artifactId>metrics-annotation</artifactId>
        <version>${dropwizard.metrics.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard.metrics</groupId>
        <artifactId>metrics-healthchecks</artifactId>
        <version>${dropwizard.metrics.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-client</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-assets</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-configuration</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-jackson</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-db</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-lifecycle</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-testing</artifactId>
        <version>${dropwizard.version}</version>
        <scope>test</scope>
      </dependency>
      <!-- Swagger v3 (OpenAPI 3) annotations, jakarta variant. The io.swagger.v3.oas.annotations.*
           types are namespace-neutral; the -jakarta artifact aligns with the Jakarta EE 9 stack and
           the io.swagger.core.v3:swagger-maven-plugin that generates the spec at build time. -->
      <dependency>
        <groupId>io.swagger.core.v3</groupId>
        <artifactId>swagger-annotations-jakarta</artifactId>
        <version>${swagger.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.jetty.ee10</groupId>
        <artifactId>jetty-ee10-servlets</artifactId>
        <version>${org.eclipse.jetty.version}</version>
      </dependency>
      <dependency>
        <groupId>redis.clients</groupId>
        <artifactId>jedis</artifactId>
        <version>${redis.clients.jedis.version}</version>
      </dependency>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-hibernate</artifactId>
        <version>${dropwizard.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.persistence</groupId>
        <artifactId>jakarta.persistence-api</artifactId>
        <version>${jakarta.persistence-api.version}</version>
      </dependency>
      <dependency>
        <groupId>org.hibernate.orm</groupId>
        <artifactId>hibernate-core</artifactId>
        <version>${hibernate-core.version}</version>
      </dependency>
      <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>${mysql.connector.j.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.jsonld-java</groupId>
        <artifactId>jsonld-java</artifactId>
        <version>${jsonld.version}</version>
        <exclusions>
          <!-- These OSGi bundles embed the whole of org.apache.http, so they put a second and older
               copy of every HttpClient 4 class on the classpath beside the one the OpenSearch REST
               client brings, and which copy resolves depends on classpath order. jsonld-java is
               served by the plain artifacts, pinned above. -->
          <exclusion>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient-osgi</artifactId>
          </exclusion>
          <exclusion>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpcore-osgi</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>nz.ac.waikato.cms.weka</groupId>
        <artifactId>weka-stable</artifactId>
        <version>${weka.version}</version>
      </dependency>
      <dependency>
        <groupId>javax.xml.ws</groupId>
        <artifactId>jaxws-api</artifactId>
        <version>${javax.ws-api.version}</version>
      </dependency>
      <dependency>
        <groupId>org.neo4j.driver</groupId>
        <artifactId>neo4j-java-driver</artifactId>
        <version>${org.neo4j.driver.version}</version>
      </dependency>
      <dependency>
        <groupId>org.glassfish.jaxb</groupId>
        <artifactId>jaxb-runtime</artifactId>
        <version>${org.glassfish.jaxb.jaxb-runtime.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.activation</groupId>
        <artifactId>jakarta.activation</artifactId>
        <version>${com.sun.activation.version}</version>
      </dependency>
      <dependency>
        <groupId>com.jayway.jsonpath</groupId>
        <artifactId>json-path</artifactId>
        <version>${com.jayway.jsonpath.version}</version>
      </dependency>
      <dependency>
        <groupId>org.javassist</groupId>
        <artifactId>javassist</artifactId>
        <version>${org.javassist.version}</version>
      </dependency>
      <dependency>
        <groupId>net.sourceforge.owlapi</groupId>
        <artifactId>owlapi-distribution</artifactId>
        <version>${net.sourceforge.owlapi.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi</artifactId>
        <version>${apache.poi.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi-ooxml</artifactId>
        <version>${apache.poi.version}</version>
      </dependency>
      <dependency>
        <groupId>org.yaml</groupId>
        <artifactId>snakeyaml</artifactId>
        <version>${org.yaml.snakeyaml.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.annotation</groupId>
        <artifactId>jakarta.annotation-api</artifactId>
        <version>2.0.0</version>
      </dependency>
      <dependency>
        <groupId>org.xerial</groupId>
        <artifactId>sqlite-jdbc</artifactId>
        <version>${org.xerial.sqlite-jdbc.version}</version>
      </dependency>
      <!-- Transitive-only artifacts, pinned so every module resolves one version. See the
           matching properties above. -->
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy</artifactId>
        <version>${transitive.byte-buddy.version}</version>
      </dependency>
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy-agent</artifactId>
        <version>${transitive.byte-buddy.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-buffer</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-codec</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-codec-http</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-common</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-handler</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-resolver</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-transport</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-transport-native-unix-common</artifactId>
        <version>${transitive.netty.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.lucene</groupId>
        <artifactId>lucene-analysis-common</artifactId>
        <version>${transitive.lucene.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.lucene</groupId>
        <artifactId>lucene-backward-codecs</artifactId>
        <version>${transitive.lucene.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.lucene</groupId>
        <artifactId>lucene-core</artifactId>
        <version>${transitive.lucene.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.lucene</groupId>
        <artifactId>lucene-queryparser</artifactId>
        <version>${transitive.lucene.version}</version>
      </dependency>
      <dependency>
        <groupId>org.glassfish.jersey.containers</groupId>
        <artifactId>jersey-container-servlet</artifactId>
        <version>${transitive.jersey.version}</version>
      </dependency>
      <dependency>
        <groupId>org.glassfish.jersey.inject</groupId>
        <artifactId>jersey-hk2</artifactId>
        <version>${transitive.jersey.version}</version>
      </dependency>
      <dependency>
        <groupId>org.glassfish.hk2</groupId>
        <artifactId>hk2-locator</artifactId>
        <version>${transitive.hk2-locator.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.jetty</groupId>
        <artifactId>jetty-http</artifactId>
        <version>${org.eclipse.jetty.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.jetty</groupId>
        <artifactId>jetty-server</artifactId>
        <version>${org.eclipse.jetty.version}</version>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>jcl-over-slf4j</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.ben-manes.caffeine</groupId>
        <artifactId>caffeine</artifactId>
        <version>${transitive.caffeine.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml</groupId>
        <artifactId>classmate</artifactId>
        <version>${transitive.classmate.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.luben</groupId>
        <artifactId>zstd-jni</artifactId>
        <version>${transitive.zstd-jni.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.errorprone</groupId>
        <artifactId>error_prone_annotations</artifactId>
        <version>${transitive.error-prone-annotations.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${transitive.protobuf-java.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.istack</groupId>
        <artifactId>istack-commons-runtime</artifactId>
        <version>${transitive.istack-commons-runtime.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-logging</groupId>
        <artifactId>commons-logging</artifactId>
        <version>${transitive.commons-logging.version}</version>
      </dependency>
      <dependency>
        <groupId>io.projectreactor</groupId>
        <artifactId>reactor-core</artifactId>
        <version>${transitive.reactor-core.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.activation</groupId>
        <artifactId>jakarta.activation-api</artifactId>
        <version>${transitive.jakarta-activation-api.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.inject</groupId>
        <artifactId>jakarta.inject-api</artifactId>
        <version>${transitive.jakarta-inject-api.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-compress</artifactId>
        <version>${transitive.commons-compress.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.angus</groupId>
        <artifactId>angus-activation</artifactId>
        <version>${transitive.angus-activation.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jboss</groupId>
        <artifactId>jandex</artifactId>
        <version>${transitive.jandex.version}</version>
      </dependency>
      <dependency>
        <groupId>org.ow2.asm</groupId>
        <artifactId>asm</artifactId>
        <version>${transitive.asm.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.enforcer.plugin.version}</version>
        <executions>
          <execution>
            <id>enforce-maven</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>${maven.version}</version>
                </requireMavenVersion>
                <!-- The app crashes at runtime on JDKs newer than 17 (Keycloak/OpenSearch), and
                     newer build JDKs have broken ByteBuddy/Hibernate init. Lock the build JVM to 17. -->
                <requireJavaVersion>
                  <version>[17,18)</version>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <!-- Generates Java classes from XML Schema for the impex and submission servers. The 4.x
             jvnet plugin emits the jakarta.xml.bind namespace; the retired org.jvnet.jaxb2.maven2
             plugin emitted javax.xml.bind and needed a jaxb-runtime held back to 2.3.x. -->
        <plugin>
          <groupId>org.jvnet.jaxb</groupId>
          <artifactId>jaxb-maven-plugin</artifactId>
          <version>${jvnet.jaxb.maven.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>${spotless.maven.plugin.version}</version>
        </plugin>
        <!-- Generates the OpenAPI description each server publishes; the version tracks the
             swagger-core artifacts the servers compile against. -->
        <plugin>
          <groupId>io.swagger.core.v3</groupId>
          <artifactId>swagger-maven-plugin-jakarta</artifactId>
          <version>${swagger.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version}</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-release-plugin</artifactId>
          <version>${maven.release.plugin.version}</version>
          <dependencies>
            <dependency>
              <groupId>org.apache.maven.scm</groupId>
              <artifactId>maven-scm-provider-git-commons</artifactId>
              <version>${maven.scm.gitexe.version}</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.plugin.version}</version>
          <configuration>
            <includes>
              <include>**/*Spec.java</include>
              <include>**/*Test.java</include>
            </includes>
            <systemPropertyVariables>
              <!-- Backend-free server tests have no application-log Redis. Queue behavior is
                   covered separately against embedded Redis; avoid one network timeout for every
                   HTTP and Cypher log event in every other test JVM. -->
              <cedar.test.suppressAppLogQueue>true</cedar.test.suppressAppLogQueue>
              <!-- Production drivers retain their defaults. Outage tests need the same exception
                   path, not thirty seconds of retry/backoff before reaching it. -->
              <cedar.test.dependencyTimeoutMillis>1000</cedar.test.dependencyTimeoutMillis>
            </systemPropertyVariables>
            <environmentVariables>
              <!-- Never inherit a developer profile's remote Redis address in a backend-free
                   suite. Tests that exercise a queue construct their own embedded-Redis config. -->
              <CEDAR_REDIS_PERSISTENT_HOST>127.0.0.1</CEDAR_REDIS_PERSISTENT_HOST>
              <CEDAR_REDIS_PERSISTENT_PORT>1</CEDAR_REDIS_PERSISTENT_PORT>
            </environmentVariables>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven.jar.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>${maven.source.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>${maven.dependency.plugin.version}</version>
          <configuration>
            <ignoreNonCompile>true</ignoreNonCompile>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>versions-maven-plugin</artifactId>
          <version>${maven.versions.plugin.version}</version>
          <configuration>
            <generateBackupPoms>false</generateBackupPoms>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>${maven.assembly.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${maven.resources.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>${maven.exec.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.jsonschema2pojo</groupId>
          <artifactId>jsonschema2pojo-maven-plugin</artifactId>
          <version>${maven.jsonschema2pojo.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>com.github.davidmoten</groupId>
          <artifactId>jax-maven-plugin</artifactId>
          <version>${maven.jax.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>build-helper-maven-plugin</artifactId>
          <version>${maven.build-helper.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${maven.deploy.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${maven.failsafe.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-install-plugin</artifactId>
          <version>${maven.install.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${maven.shade.plugin.version}</version>
          <dependencies>
            <!-- Merges the Log4j2Plugins.dat plugin registries of all shaded dependencies.
                 Without it, one dependency's small registry wins the merge, log4j-core's own
                 converters never register, and the server logs a wall of "Unrecognized format
                 specifier" errors at startup. -->
            <dependency>
              <groupId>org.apache.logging.log4j</groupId>
              <artifactId>log4j-transform-maven-shade-plugin-extensions</artifactId>
              <version>${log4j.transform.shade.extensions.version}</version>
            </dependency>
          </dependencies>
          <configuration>
            <createDependencyReducedPom>true</createDependencyReducedPom>
            <filters>
              <filter>
                <artifact>*:*</artifact>
                <excludes>
                  <exclude>META-INF/*.SF</exclude>
                  <exclude>META-INF/*.DSA</exclude>
                  <exclude>META-INF/*.RSA</exclude>
                  <!-- A dependency's module descriptor is meaningless in a classpath uber-jar and is
                       what makes shade log "Discovered module-info.class. Shading will break its
                       strong encapsulation" for ~every jar. Dropping it is shade's own recommendation
                       and removes that whole class of warning. -->
                  <exclude>module-info.class</exclude>
                  <exclude>META-INF/versions/*/module-info.class</exclude>
                  <!-- Per-dependency Maven build descriptors (pom.xml / pom.properties). Pure build
                       metadata, present in nearly every jar, so a large source of overlapping-resource
                       warnings; nothing at runtime reads them out of the uber-jar. -->
                  <exclude>META-INF/maven/**</exclude>
                </excludes>
              </filter>
            </filters>
          </configuration>
          <executions>
            <!-- Shared shade execution for the CEDAR server application jars. Inert unless a
                 pom sets cedar.shade.phase to "package"; see the property declaration. -->
            <execution>
              <id>cedar-shade</id>
              <phase>${cedar.shade.phase}</phase>
              <goals>
                <goal>shade</goal>
              </goals>
              <configuration>
                <transformers>
                  <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                  <transformer implementation="org.apache.logging.log4j.maven.plugins.shade.transformer.Log4j2PluginCacheFileTransformer"/>
                  <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                    <mainClass>${cedar.shade.main.class}</mainClass>
                  </transformer>
                </transformers>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${maven.javadoc.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-clean-plugin</artifactId>
          <version>${maven.clean.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-site-plugin</artifactId>
          <version>${maven.site.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>jaxb2-maven-plugin</artifactId>
          <version>${jaxb2.maven.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <profiles>
    <profile>
      <id>maven-central-release</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>${maven.source.plugin.version}</version>
            <executions>
              <execution>
                <id>attach-sources</id>
                <goals>
                  <goal>jar-no-fork</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>${maven.javadoc.plugin.version}</version>
            <executions>
              <execution>
                <id>attach-javadocs</id>
                <goals>
                  <goal>jar</goal>
                </goals>
                <configuration>
                  <failOnError>false</failOnError>
                  <doclint>none</doclint>
                  <quiet>true</quiet>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>${maven.gpg.plugin.version}</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>${maven.central-publishing.plugin.version}</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <deploymentName>${project.artifactId}:${project.version}</deploymentName>
              <autoPublish>true</autoPublish>
              <waitUntil>published</waitUntil>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
