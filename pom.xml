<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.metadatacenter</groupId>
  <artifactId>cedar-parent</artifactId>
  <version>0.1.0</version>
  <packaging>pom</packaging>

  <name>CEDAR Parent</name>
  <description>Specify common data</description>

  <properties>
    <java.version>1.8</java.version>

    <scala.version>2.11.5</scala.version>
    <scala.dependencies.version>2.11</scala.dependencies.version>
    <play2.version>2.3.8</play2.version>
    <play2.scala.version>2.11</play2.scala.version>
    <play-java-ws.version>2.3.8</play-java-ws.version>

    <apache.httpclient.version>4.5.1</apache.httpclient.version>
    <mongodb.version>3.0.0</mongodb.version>
    <jackson.core.version>2.5.0</jackson.core.version>
    <checker.framework.version>1.7.5</checker.framework.version>

    <junit.version>4.12</junit.version>
    <fge.version>2.2.6</fge.version>
    <slf4j.version>1.7.14</slf4j.version>
    <logback.version>1.1.3</logback.version>

    <keycloak.version>1.8.0.Final</keycloak.version>


    <scala.maven.plugin.version>3.1.6</scala.maven.plugin.version>
    <play2.maven.plugin.version>1.0.0-beta1</play2.maven.plugin.version>
    <maven.surefire.plugin.version>2.16</maven.surefire.plugin.version>
    <maven.compiler.plugin.version>3.1</maven.compiler.plugin.version>

    <org.metadatacenter.cedar.server.utils.version>0.1.0</org.metadatacenter.cedar.server.utils.version>
    <org.metadatacenter.cedar.server.utils.core.version>0.1.0</org.metadatacenter.cedar.server.utils.core.version>
    <org.metadatacenter.cedar.template.operations.version>0.1.0</org.metadatacenter.cedar.template.operations.version>
    <org.metadatacenter.cedar.auth.operations.version>0.1.0</org.metadatacenter.cedar.auth.operations.version>
    <org.metadatacenter.cedar.user.operations.version>0.1.0</org.metadatacenter.cedar.user.operations.version>

    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
  </properties>

  <repositories>

    <repository>
      <id>typesafe-releases</id>
      <url>http://repo.typesafe.com/typesafe/releases/</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>

  </repositories>

  <pluginRepositories>

    <pluginRepository>
      <id>typesafe-releases-plugins</id>
      <url>http://repo.typesafe.com/typesafe/releases/</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </pluginRepository>

    <pluginRepository>
      <id>sonatype-nexus-snapshots</id>
      <name>Sonatype Nexus Snapshots</name>
      <url>https://oss.sonatype.org/content/repositories/snapshots</url>
      <releases>
        <enabled>false</enabled>
      </releases>
    </pluginRepository>

  </pluginRepositories>


  <build>
    <sourceDirectory>src/main/java</sourceDirectory>
    <testSourceDirectory>src/test/java</testSourceDirectory>
    <pluginManagement>
      <plugins>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version}</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
          </configuration>
        </plugin>

        <plugin>
          <groupId>com.google.code.play2-maven-plugin</groupId>
          <artifactId>play2-maven-plugin</artifactId>
          <version>${play2.maven.plugin.version}</version>
          <extensions>true</extensions>
        </plugin>

        <plugin>
          <groupId>net.alchim31.maven</groupId>
          <artifactId>scala-maven-plugin</artifactId>
          <version>${scala.maven.plugin.version}</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
            <checkMultipleScalaVersions>false</checkMultipleScalaVersions>
          </configuration>
          <executions>
            <execution>
              <goals>
                <goal>compile</goal>
                <goal>testCompile</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.plugin.version}</version>
          <configuration>
            <includes>
              <include>**/*Spec.java</include>
              <include>**/*Test.java</include>
            </includes>
          </configuration>
        </plugin>

      </plugins>
    </pluginManagement>
  </build>

</project>
